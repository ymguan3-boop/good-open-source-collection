# yaml-language-server: $schema=https://aka.ms/winget-manifest.version.1.6.0.schema.json
# Reference copy. CI (winget-releaser in .github/workflows/release.yml) regenerates
# and submits the per-version manifests to microsoft/winget-pkgs on every release,
# so PackageVersion here may lag the published package. See the "How CI keeps it
# current" section of README.md in this directory for the version-lag policy.
PackageIdentifier: OpenGeos.GeoLibre
PackageVersion: 1.5.0
DefaultLocale: en-US
ManifestType: version
ManifestVersion: 1.6.0
